@model List<ProductOverviewModel>
@using Nop.Core
@using Nop.Core.Domain.Orders
@using Nop.Core.Domain.Tax
@using Nop.Core.Infrastructure
@using Nop.Web.Models.Catalog;

@using Nop.Web.Models.Catalog;


<style>
    .just-in-heading {
        color: black;
        /* height: 113px; */
        /* position: absolute; */
        /* left: 139px; */
        font-size: 12px;
        font-weight: bolder;
        text-align: center;
    }
</style>
@if (Model.Count > 0)
{
    <div class="custom-row">
        @foreach (var item in Model)
        {<div class="col-xs-18 col-sm-6 col-md-4 cutom-thumbnail" style="padding-bottom: 15px">
            <div>
                <div style="height:119px;overflow:hidden">
                    <img src="@item.DefaultPictureModel.ImageUrl">
                </div>
                <span>@item.Name</span>
            </div>
        </div>}

    </div>
}