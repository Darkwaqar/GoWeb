@model ShoppingCartModel.GiftCardBoxModel
@using Nop.Web.Models.ShoppingCart;
@if (Model.Display)
{
    @*<div class="row marginTop10">
            <div class="col-sm-8">
                <h4>@T("ShoppingCart.GiftCardCouponCode")</h4>
                <div class="text-muted marginBottom10">
                    @T("ShoppingCart.GiftCardCouponCode.Tooltip")
                </div>
                <div class="input-group">
                    <input name="giftcardcouponcode" type="text" class="form-control" />
                    <div class="input-group-btn">
                        <input type="submit" name="applygiftcardcouponcode" value="@T("ShoppingCart.GiftCardCouponCode.Button")" class="btn btn-default" />
                    </div>
                </div>
                @if (!String.IsNullOrEmpty(Model.Message))
                {
                    <div class="@(Model.IsApplied ? "text-success" : "text-danger")">
                       <small>@Model.Message</small>
                    </div>
                }
            </div>
        </div>*@
    <div class="summary giftcardcode">
        <div class="uppercase " style="font-weight:bold">
            GIFT CARD
        </div>
        <div class="col-md-5 col-xs-12 nopadding">
            <input name="giftcardcouponcode" type="text" class="textbox" placeholder="ENTER YOUR CODE" style="height:30px" />
            @*<div class="text-center">*@
        </div>
        <div class="col-md-2 nopadding">
            <input type="submit" name="applygiftcardcouponcode" value="REDEEM" class="btn-big wfull hidden-xs hidden-sm" style="height:31px;width:108px;margin-top:10px" />
        </div>
           <center class="hidden-md hidden-lg"><input type="submit" name="applygiftcardcouponcode" value="REDEEM" class="btn-big wfull" style="margin-left:-4px;height:31px;width:108px;margin-top:-3px" /></center>
           <br>
        <br>
        <br>
      
        @if (!String.IsNullOrEmpty(Model.Message))
        {
            <div class="@(Model.IsApplied ? "text-success" : "text-danger")">
                <small>@Model.Message</small>
            </div>
        }
    </div>
}