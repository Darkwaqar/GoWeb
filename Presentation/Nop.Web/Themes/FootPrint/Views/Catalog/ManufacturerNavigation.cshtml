@model ManufacturerNavigationModel
@using Nop.Web.Models.Catalog;
@if (Model.Manufacturers.Count > 0)
{
    <nav class="navbar navbar-default panel panel-default left-custom-panel" role="navigation">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#manufacturers-sidebar" style="background-color:linen">
                
                <span class="fa fa-arrow-down " ></span>
            </button>
            <a class="navbar-brand visible-xs" href="#">@T("Manufacturers")</a>
        </div>
        <div class="panel-heading hidden-xs">
            <h3 class="panel-title">@T("Manufacturers")</h3>
        </div>
        <div class="navbar-collapse collapse" id="manufacturers-sidebar">
            <div class="panel-body">
                <ul class="list-unstyled">
                    @foreach (var manufacturer in Model.Manufacturers)
                    {
                        <li class="@(manufacturer.IsActive ? "active" : "inactive")">
                            <a href="@Url.RouteUrl("Manufacturer", new { SeName = manufacturer.SeName })">@manufacturer.Name</a>
                        </li>
                    }
                </ul>
                @if (Model.TotalManufacturers > Model.Manufacturers.Count)
                {
                    <small class="view-all pull-right">
                        <a href="@Url.RouteUrl("ManufacturerList")">@T("Manufacturers.ViewAll")</a>
                    </small>
                }
            </div>
        </div>
    </nav>
}