@model QueuedFcmModel
@using Nop.Admin.Models.Fcm

@{
    //page title
    ViewBag.Title = T("Admin.System.QueuedFcms.EditQueuedFcmDetails").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Queued fcms");
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.System.QueuedFcms.EditQueuedFcmDetails")
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Admin.System.QueuedFcms.BackToList").Text, "List")</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="requeue" class="btn bg-green">
                <i class="fa fa-clone"></i>
                @T("Admin.System.QueuedFcms.Requeue")
            </button>
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            <span id="queuedFcm-delete" class="btn bg-red">
                <i class="fa fa-trash-o"></i>
                @T("Admin.Common.Delete")
            </span>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}
@Html.DeleteConfirmation("queuedFcm-delete")
